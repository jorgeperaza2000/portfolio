
<section>
    <div class="container">
        <div class="info-text">
            <!-- <h1>
                Tu visión, mi código.<br/>
                <span>Juntos</span>, lo hacemos<br/>
                <span>posible</span>.
            </h1> -->
            <!-- <h1>
                Empieza con una <span>IDEA</span>,<br/>
                termina con una <span>OBRA MAESTRA</span>.
            </h1> -->
            <h1>
                Start with an <span>IDEA</span>,<br/>
                ends with a <span>MASTERPIECE</span>.
            </h1>
            <!-- <h1>
                Hazte <span>visible</span>.<br/>
                Hazte <span>digital</span>.<br/>
                Desarrollo web a tu <span>medida</span>.
            </h1> -->
        </div>
        <div class="info-picture">
            <img
                style="width:80%"
                src="/images/perfil.png"
                alt=""
            />
        </div>
    </div>
</section>
<style>
    .container {
        display: grid;
        grid-template-columns: 50% 50%;
        justify-content: center;
        align-items: center;
        width: 100%;
        height: 100%;
        max-width: 1280px;
        margin: 3rem auto 0;
    }
    /* img {
        position: relative;
        z-index: -1;
    } */
    img:hover {
        -webkit-filter: drop-shadow(0 0 80px var(--green));
        filter: drop-shadow(0 -30px 80px var(--green));
    }
    .info-text {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        height: 100%;
    }
    h1 {
        font-family: var(--syne);
        color: var(--text);
        font-size: 3rem;
        text-align: center;
        white-space: nowrap;
    }
    h1 span {
        background-image: var(--gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    .info-picture {
        display: flex;
        align-items: flex-end;
        justify-content: flex-end;
    }
</style>